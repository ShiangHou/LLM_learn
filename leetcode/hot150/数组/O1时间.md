# O1时间插入、删除、获取随机元素

## 题目描述

实现RandomizedSet 类：

RandomizedSet() 初始化 RandomizedSet 对象
bool insert(int val) 当元素 val 不存在时，向集合中插入该项，并返回 true ；否则，返回 false 。
bool remove(int val) 当元素 val 存在时，从集合中移除该项，并返回 true ；否则，返回 false 。
int getRandom() 随机返回现有集合中的一项（测试用例保证调用此方法时集合中至少存在一个元素）。每个元素应该有 相同的概率 被返回。
你必须实现类的所有函数，并满足每个函数的 平均 时间复杂度为 O(1) 。

总结一下，一共需要实现几个方法

首先是insert 不存在的话就插入，存在的话就返回false

remove也是

get random的意思是随机获取一个

由于全部都要是O1，所以肯定是hash

但是hash无法满足随机数的情况

所以这里用一个hash+nums来去实现

准备一个数组nums，准备一个hash的dict

dict里面存的key是数字，value是对应的nums的下标

如果是插入的话，先判断在不在dict里面，然后插入，把数组的最后一个append一下，然后返回出数组的长度的+1（也就是last），然后存在dict种

也就是
        self.indices[val] = len(self.nums)
        self.nums.append(val)
        即来一个就直接往数组后面放着



也就是说，新的val的插入存在dict里面是len(nums)(因为是从0开始的)

删除的话，逻辑见下面

        id = self.indices[val]
        self.nums[id] = self.nums[-1]
        self.indices[self.nums[id]] = id
        self.nums.pop()
        del self.indices[val]

思考一下，

首先是从hash中拿到要删除的下标 id

然后在数组中把这个删除掉，我把数组中的最后一个元素值赋给id的位置

那么同时，这个在hash中的对应的key-val的val也要变成id（原来是最后一个）

最后把最后一个pop

然后del hash就行

比如说数组是 3 5 8 2 

hash中 是{5:1}，{2:3}

我要删除5，首先记录5的id是1

然后变成 3 2 8 2，相当于要把最后一个2的位置换到被删除的
然后变成{2:1} ,这里的1就是那个id

最后del掉hash就行

最后一个随机选，就是choice就行

```python 
class RandomizedSet:

    def __init__(self):
        self.nums = []
        self.dict = {}

    def insert(self, val: int) -> bool:
        if val in self.dict:
            return False
        self.dict[val] = len(self.nums)
        self.nums.append(val)
        return True

    def remove(self, val: int) -> bool:
        if val not in self.dict:
            return False
        #首先记录要删除的nums的位置
        _id = self.dict[val]
        #然后把最后一个元素放在这上面包括hash和nums
        self.nums[_id] = self.nums[-1]
        self.dict[self.nums[_id]] = _id#改一下hash

        #直接删除最后一个，包括hash和nums
        self.nums.pop()
        del self.dict[val]
        return True

    def getRandom(self) -> int:
        return choice(self.nums)


# Your RandomizedSet object will be instantiated and called as such:
# obj = RandomizedSet()
# param_1 = obj.insert(val)
# param_2 = obj.remove(val)
# param_3 = obj.getRandom()

```
