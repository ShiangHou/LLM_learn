# 京东实习项目

## SFT微调

主要是做了各种业务场景领域下的微调

那么微调项目主要需要知道几点

首先是数据，怎么清洗、怎么打标、怎么定义高质量，怎么进行数据分布，针对长尾查询，做了哪些特殊处理，为什么这么做

其次是模型选用，为什么用8B，qwen还有那些参数大小的模型，

然后是训练细节，全参还是Lora，微调框架用的是什么，微调需要的显卡资源计算，超参数是怎么设置的，为什么这么设置

然后是测试，baseline是什么，这里说的选用的baseline是多大的模型，这种性能差异是源于知识注入，还是指令遵循能力的格式对齐？


### 数据的清洗
数据清洗，一般情况下我们在寻求数分或者运营同学去做表或者数据给我们
数据的话，我们会先根据运营或者其他同学给出的规则，去写Prompt，对数据的大致分布有一个了解，然后会使用大模型交叉打标，针对一致的留下来，不一致的交由外包的来去审查，然后针对长尾的，主要是针对一些真实数据比例很少的做了增强和补充，补充道和other标签的数量一致，


完美的回答是，

1：数据获取与规则化清洗

针对数据，需要参与意图分类的规则的界限，共同制定打标规范，过程就是运营和产研先出方案，我们参与其中制定，制定标准一个是按照运营流程，以及标签的边界

其次数据的规则化清洗，一般是采用启发式规则去除一些无意义的乱码（短文本暂时不能去除，有些业务短文本也是有意义）确保纯净度，

2：自动化与人工协同打标

首先让两个大模型进行打标，高度一致的列入候选样本

对于不一致或者一些边界样本，让人工进行仲裁或者重新打标

3：长尾分布处理与数据增强**重要**
金融场的业务领域长尾分布一般十分严重，真实的数据分布中，2个意图占整个50%

针对这种长尾分布，主要是想保持分布的平滑，首先是头部，占比压缩到30%左右，然后尾部弄到1-2%，至少要比other要多，最终的头尾样本比压缩到10：1左右

增强样本的方法，主要是填补一些边缘的标签分布，以及

给定一些种子词，比如产品名称等，然后去写prompt，让模型生成各种角色下的query，为了防止生成的query重复，还会去把生成的与之前的相匹配，相似度高的直接舍弃不用

追问，为什么要弄成平滑的分布，而不是弄成各个标签的分布比率是一样的


回答：扩展数据是为了让训练的时候每个batch都能够有足够的可见度，，防止模型一直回答一个答案，以及灾难性遗忘

而不把各个标签分布弄成一致的，是为了兼顾业务分布，因为长尾是线上真实的数据分布，如果都强行在训练的时候拉平意图，破坏业务先验概率：模型在训练时会隐式学习数据的先验分布。真实的线上环境依然是高频意图占主导。如果强行把 20 个标签拉平，模型会错误地认为所有查询发生的概率一样，导致上线后对核心高频业务的误判率激增

此外，如果拉平的话，那些占据绝大多数比率的就要下降，失大量真实丰富的用户特征，要么把长尾强行扩充几十倍（导致模型在由模板生成的同质化数据上严重过拟合


### 模型选用和训练细节

用的什么模型作为基座模型，为什么用，用的什么训练框架，用的什么训练，多少张显卡，时间，一些超参数是多少

我主要用的是qwen3 8B，（需要详细介绍一下Qwen3 系列，以及采用的是什么架构）

训练用的lora（lora的原理准备，然后手搓，lora的初始化，qlora，为什么用lora不用全参，lora好处，等等）

多少张显卡（6张L20显卡），怎么计算训练的时候显存占用大小（对比Lora和全参）

用的什么训练框架  llama-factory，有没有用过其他的比如ms swift等

训练的超参数有哪些，deep speed，vllm推理框架，超参数的含义，怎么配置

用llama-factory的流程，数据在哪里传，等等


此处需要知道的较多，一个是qwen 3 系列的模型架构要知道，然后lora要详细再详细的知道，此外模型训练的参数，deepseed